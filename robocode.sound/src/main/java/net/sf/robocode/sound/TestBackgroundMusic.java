package net.sf.robocode.sound;


/**
 * Test unit for background music
 * 
 * 
 * @author Team MCJJ 
 * 
 */

/*import static org.mockito.Mockito.*;
import static org.junit.Assert.*;

import org.junit.Test;
import org.mockito.Mockito;


public class TestBackgroundMusic {

	BackgroundMusicTracker b = Mockito.mock(BackgroundMusicTracker.class);
	
	void setup(){
		Mockito.when(b.music == true).thenReturn(true);
		Mockito.when(b.music == false).thenReturn(false);
		Mockito.when(b.clip.open()).thenReturn(b.clip.getFormat());
		Mockito.when(b.clip.open()).thenReturn(true);
	}
	
	void test(){
		assertEquals(true, b.clip.open());
		assertEquals(".wav",b.clip.open());
		
	}
}
*/